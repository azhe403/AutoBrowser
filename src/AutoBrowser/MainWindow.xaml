<ui:FluentWindow x:Class="AutoBrowser.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:views="clr-namespace:AutoBrowser.Views"
                 mc:Ignorable="d"
                 Title="AutoBrowser - URL Router" Icon="/app.ico" Height="650" Width="950"
                 MinWidth="600" MinHeight="400"
                 ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
                 ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 ExtendsContentIntoTitleBar="True"
                 Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterScreen">
    <ui:FluentWindow.Resources>
    </ui:FluentWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="AutoBrowser - URL Router" Icon="/app.ico"/>

        <Grid Grid.Row="1" Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Sliced: Toolbar -->
            <views:ToolbarView Grid.Row="0" Margin="0,0,0,4"/>

            <!-- Sliced: Rule list -->
            <views:RulesListView Grid.Row="1"/>
        </Grid>

        <!-- Sliced: Fallback & Tray Settings -->
        <views:FooterView Grid.Row="2" Margin="8,0,8,4"/>

        <!-- Sliced: Status bar -->
        <views:StatusControl Grid.Row="3" Margin="8,0,8,8"/>
    </Grid>
</ui:FluentWindow>
