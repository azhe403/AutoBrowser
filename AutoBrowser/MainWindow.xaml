<Window x:Class="AutoBrowser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AutoBrowser - URL Router" Icon="/app.ico" Height="650" Width="950"
        MinWidth="600" MinHeight="400"
        StateChanged="Window_StateChanged">
    <Window.Resources>
        <Style TargetType="Button" x:Key="ToolBarButton">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" BandIndex="0">
            <Button Content="Add Rule" Style="{StaticResource ToolBarButton}"
                    Command="{Binding AddRuleCommand}"/>
            <Button Content="Edit Rule" Style="{StaticResource ToolBarButton}"
                    Command="{Binding EditRuleCommand}"/>
            <Button Content="Delete Rule" Style="{StaticResource ToolBarButton}"
                    Command="{Binding DeleteRuleCommand}"/>
            <Separator/>
            <Button Content="Move Up" Style="{StaticResource ToolBarButton}"
                    Command="{Binding MoveUpCommand}"/>
            <Button Content="Move Down" Style="{StaticResource ToolBarButton}"
                    Command="{Binding MoveDownCommand}"/>
            <Separator/>
            <CheckBox Content="autobrowser:// protocol" IsChecked="{Binding IsProtocolRegistered}"
                      Margin="4,0"/>
            <CheckBox Content="Registered as default browser" IsChecked="{Binding IsDefaultBrowser}"
                      Margin="4,0"/>
            <Separator/>
            <Button Content="Test URL..." Style="{StaticResource ToolBarButton}"
                    Command="{Binding LaunchUrlCommand}"/>
        </ToolBar>

        <!-- Status bar -->
        <StatusBar Grid.Row="1" Margin="0,4,0,4">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Rule list -->
        <ListView x:Name="RuleListView" Grid.Row="2"
                  ItemsSource="{Binding Rules}"
                  SelectedItem="{Binding SelectedRule}"
                  SelectionMode="Single"
                  MouseDoubleClick="RuleListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Enabled" Width="60">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsEnabled}" Checked="CheckBox_Toggled" Unchecked="CheckBox_Toggled"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Priority" Width="60"
                                    DisplayMemberBinding="{Binding Priority}"/>
                    <GridViewColumn Header="Name" Width="200"
                                    DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="URL Pattern" Width="250"
                                    DisplayMemberBinding="{Binding UrlPattern}"/>
                    <GridViewColumn Header="Browser" Width="250"
                                    DisplayMemberBinding="{Binding BrowserPath}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
