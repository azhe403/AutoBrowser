# Changes — 2026-07-04

## Theme ComboBox replaced with ToggleSwitch
- **Files changed**: `MainWindow.xaml`, `MainViewModel.cs`, `App.xaml.cs`, `AppThemeMode.cs`, `AppSettings.cs`
- ComboBox (System/Light/Dark) → `ui:ToggleSwitch` bound to `IsDarkTheme` (bool)
- `AppThemeMode.System` removed
- `App.CurrentThemeMode` property added — ViewModel reads this instead of loading settings

## Architecture memory split
- Single `architecture.md` → 4 focused memories: `AutoBrowser/architecture`, `AutoBrowser/flow`, `AutoBrowser/services`, `AutoBrowser/ui-behavior`

## Removed message box on default browser toggle
- `MainViewModel.IsDefaultBrowser` setter — registers/unregisters silently

## Status bar now bound to ViewModel
- **Files changed**: `MainViewModel.cs`, `MainWindow.xaml`, `MainWindow.xaml.cs`
- Added `Status` property (string) to `MainViewModel`, default `"Ready"`
- XAML `TextBlock` now binds `{Binding Status}` instead of `x:Name`
- Status messages emitted from: Add/Edit/Delete rule, Move Up/Down, Theme toggle, Protocol register/unregister, Default browser register/unregister, Test URL routing result