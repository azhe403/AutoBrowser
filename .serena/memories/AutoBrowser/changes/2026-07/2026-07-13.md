# Changes 2026-07-13

## Delete Rule UI Test Added
- Added `MainWindow_DeleteRule` integration test in `src/AutoBrowser.Tests/UI/MainWindowTests.cs` using FlaUI
- The test validates launching the app, adding a rule ("Rule To Delete"), selecting it, clicking the Delete button, and asserting that the rule is no longer present in the UI
