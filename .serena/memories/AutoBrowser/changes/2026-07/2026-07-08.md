# 2026-07-08 Changes

## New Features
- **Single-instance with tray restore** — Named pipe IPC, mutex, `WindowForegroundHelper`
- **Push notifications** — `ShowNotification()` for all URL routing results
- **Update check throttling** — Once per hour via `LastUpdateCheckTime` in settings
- **Re-register prompt** — Detects moved app, shows dialog to re-register protocol/default browser

## Bug Fixes
- **BrowserDefinition self-exclusion** — `Path.GetFileName()` comparison instead of full path
- **Mutex disposed too early** — Made class-level `_mutex` field, disposed in `OnExit`
- **Title bar non-movable after MessageBox** — Switched to WPF UI `MessageBox` with `ShowDialogAsync()` in `CheckAndPromptReRegister()`

## Refactoring
- **App.xaml.cs** — Refactored into focused helper methods
- **WindowForegroundHelper** — Extracted Win32 P/Invoke into separate helper

## Files Modified
- `App.xaml.cs`, `MainWindow.xaml.cs`, `Helpers/WindowForegroundHelper.cs`
- `Models/AppSettings.cs`, `Models/BrowserDefinition.cs`
- `Services/ProtocolService.cs`, `Services/IProtocolService.cs`
- `Services/DefaultBrowserService.cs`, `Services/IDefaultBrowserService.cs`
- `AGENTS.md`, `README.md`
